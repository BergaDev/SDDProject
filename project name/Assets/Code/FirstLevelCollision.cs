using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Collision : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //initalise variables
        int lives = 0; //lives left, decreases on character death
        int time = 0; //counts time
        int speedmult = 0; //multiplies speed
        int level = 0; //updates each level
        int death = 0; //updates each time you die
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
